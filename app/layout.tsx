import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "antd/dist/reset.css";
import "./globals.css";
import { Footer } from "./sessions/footer.session";
import { AboutSession } from "./sessions/about.session";
import { BuilderSession } from "./sessions/builder.session";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>{/* Metadata or other head elements */}</head>
      <body className={`${inter.variable} antialiased max-w-[1250px] mx-auto`}>
        <AboutSession />
        <BuilderSession />
        <Footer />
      </body>
    </html>
  );
}
